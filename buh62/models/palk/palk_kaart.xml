<?xml version = "1.0" encoding="Windows-1252" standalone="yes"?>
<VFPData>
  <grid>
    <sql>select a.*, $2::integer as userId
            from palk.cur_palk_kaart a
            where (rekvid = $1 or rekvid is null)</sql>
    <alias>curPalkKaart</alias>
  </grid>
  <select>
    <sql>SELECT
                  $2 :: INTEGER            AS userid,
                 'PALK_KAART' AS doc_type_id,
                  pk.id,
                  pk.parentid,
                  pk.lepingid,
                  pk.libid,
                  pk.summa,
                  pk.percent_,
                  pk.tulumaks,
                  pk.tulumaar,
                  pk.status,
                  pk.muud,
                  pk.alimentid,
                  pk.tunnus                                             AS tunnus,
                  pk.minsots,
                  osakond.kood                                          AS osakond,
                  osakond.id                                            AS osakondId,
                  amet.kood                                             AS amet,
                  l.kood,
                  l.nimetus,
                  (l.properties :: JSONB -&gt;&gt; 'liik') :: INTEGER         AS liik,
                  (l.properties :: JSONB -&gt;&gt; 'tund') :: INTEGER         AS tund,
                  (l.properties :: JSONB -&gt;&gt; 'maks') :: INTEGER         AS maks,
                  (l.properties :: JSONB -&gt;&gt; 'asutusest') :: INTEGER    AS asutusest,
                  (l.properties :: JSONB -&gt;&gt; 'tululiik') :: VARCHAR(20) AS tululiik,
                  coalesce(v.valuuta, 'EUR') :: VARCHAR                 AS valuuta,
                  coalesce(v.kuurs, 1) :: NUMERIC                       AS kuurs
                FROM libs.library l
                  INNER JOIN palk.palk_kaart pk ON pk.libId = l.id
                  INNER JOIN palk.tooleping t ON pk.lepingId = t.id
                  INNER JOIN libs.library amet ON amet.id = t.ametid
                  INNER JOIN libs.library osakond ON osakond.id = t.osakondid
                  LEFT OUTER JOIN docs.dokvaluuta1 v
                    ON pk.id = v.dokid AND v.dokliik = array_position((enum_range(NULL :: DOK_VALUUTA)), 'palk_kaart')
                WHERE pk.id = $1</sql>
    <sqlAsNew>SELECT
                      $1 :: INTEGER        AS id,
                      $2 :: INTEGER        AS userid,
                      'PALK_KAART'        AS doc_type_id,
                      0 :: INTEGER        AS parentid,
                      0 :: INTEGER        AS lepingid,
                      0 :: INTEGER        AS libid,
                      0 :: NUMERIC(14, 4) AS summa,
                      0 :: INTEGER        AS percent_,
                      0 :: INTEGER        AS tulumaks,
                      0 :: NUMERIC        AS tulumaar,
                      1 :: INTEGER        AS status,
                      NULL :: TEXT        AS muud,
                      0 :: INTEGER        AS alimentid,
                      NULL :: VARCHAR(20) AS tunnus,
                      0 :: INTEGER        AS minsots,
                      'EUR' :: VARCHAR    AS valuuta,
                      1 :: NUMERIC        AS kuurs</sqlAsNew>
    <query />
    <multiple>false</multiple>
    <alias>row</alias>
  </select>
  <selectAsLibs>
    <sql>select * from palk.com_palk_kaart a 
        where (rekvid = $1 or rekvid is null)</sql>
    <alias>selectAsLibs</alias>
  </selectAsLibs>
  <saveDoc>
    <sql>select palk.sp_salvesta_palk_kaart($1, $2, $3) as id</sql>
    <alias>saveDoc</alias>
  </saveDoc>
  <deleteDoc>
    <sql>select error_code, result, error_message from palk.sp_delete_palk_kaart($1, $2)</sql>
    <alias>deleteDoc</alias>
  </deleteDoc>
  <requiredFields>
    <validate>libid,lepingid</validate>
  </requiredFields>
  <executeSql>
    <sql></sql>
    <alias></alias>
  </executeSql>
  <executeCommand>
    <sql>select error_code, result, error_message from palk.change_kaart_status($1, $2)</sql>
    <alias>changeStatus</alias>
  </executeCommand>
  <register>
    <sql></sql>
    <alias></alias>
  </register>
  <endProcess>
    <sql />
    <alias />
  </endProcess>
  <generateJournal>
    <sql />
    <alias />
  </generateJournal>
  <print></print>
</VFPData>
