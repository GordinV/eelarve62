<?xml version = "1.0" encoding="Windows-1252" standalone="yes"?>
<VFPData>
  <grid>
    <sql>select id, coalesce(kood,'')::varchar(20) as kood, coalesce(nimetus,'')::varchar(254) as nimetus,  $2::integer as userId, dok
            from libs.nomenklatuur n
            where (n.rekvId = $1 or n.rekvid is null) and n.status &lt;&gt; 3</sql>
    <alias>curNomenklatuur</alias>
  </grid>
  <select>
    <sql>select n.kood, n.id, n.nimetus, n.dok, n.muud, n.rekvid, $2::integer as userid, 'NOMENCLATURE' as doc_type_id,
                'EUR' as valuuta, 1 as kuurs,
                n.uhik as uhik,
                n.hind as hind,
                (n.properties::jsonb -&gt;&gt;'vat')::varchar(20) as vat,
                (n.properties::jsonb -&gt;&gt;'konto')::varchar(20) as konto,
                (n.properties::jsonb -&gt;&gt;'projekt')::varchar(20) as projekt,
                (n.properties::jsonb -&gt;&gt;'tunnus')::varchar(20) as tunnus,
                (n.properties::jsonb -&gt;&gt;'tegev')::varchar(20) as tegev,
                (n.properties::jsonb -&gt;&gt;'allikas')::varchar(20) as allikas,
                (n.properties::jsonb -&gt;&gt;'rahavoog')::varchar(20) as rahavoog,
                (n.properties::jsonb -&gt;&gt;'artikkel')::varchar(20) as artikkel                
                from libs.nomenklatuur n 
                where n.id = $1</sql>
    <sqlAsNew>select  $1::integer as id , $2::integer as userid, 'NOMENCLATURE' as doc_type_id,
            ''::varchar(20) as  kood,
            0::integer as rekvid,
            ''::varchar(254) as nimetus,
            ''::varchar(20) as dok,
            ''::varchar(20) as uhik,
            0::numeric as hind,
            0::numeric as ulehind,
            1::numeric as kogus,
            null::text as formula,
            0::integer as status,
            null::text as muud,
            null::text as properties,
            'EUR' as valuuta, 1 as kuurs,
            '20'::varchar(20) as vat,
            null::varchar(20) as konto,
            null::varchar(20) as projekt,
            null::varchar(20) as tunnus,
            null::varchar(20) as tegev,
            null::varchar(20) as allikas,
            null::varchar(20) as rahavoog,
            null::varchar(20) as artikkel</sqlAsNew>
    <query />
    <multiple>false</multiple>
    <alias>row</alias>
  </select>
  <selectAsLibs>
    <sql>select * from com_nomenclature 
            where (rekvid = $1 or rekvid is null)
            order by kood</sql>
    <alias>selectAsLibs</alias>
  </selectAsLibs>
  <saveDoc>
    <sql>select libs.sp_salvesta_nomenclature($1, $2, $3) as id</sql>
    <alias>saveDoc</alias>
  </saveDoc>
  <deleteDoc>
    <sql>select error_code, result, error_message from libs.sp_delete_nomenclature($1::integer, $2::integer)</sql>
    <alias>deleteDoc</alias>
  </deleteDoc>
  <requiredFields>
    <validate>kood,nimetus,dok</validate>
  </requiredFields>
  <executeSql>
    <sql></sql>
    <alias></alias>
  </executeSql>
  <executeCommand>
    <sql></sql>
    <alias></alias>
  </executeCommand>
  <register>
    <sql></sql>
    <alias></alias>
  </register>
  <endProcess>
    <sql />
    <alias />
  </endProcess>
  <generateJournal>
    <sql />
    <alias />
  </generateJournal>
  <print></print>
</VFPData>
