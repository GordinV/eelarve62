<?xml version = "1.0" encoding="Windows-1252" standalone="yes"?>
<VFPData>
  <grid>
    <sql>select id, trim(kood)::varchar(20) as kood, trim(nimetus)::varchar(254) as nimetus,  $2::integer as userId,
            case when l.tun5 = 1 then 'SD' when l.tun5 = 2 then 'SK' when l.tun5 = 3 then 'D' when l.tun5 = 4 then 'K' else null end::varchar(20) as konto_tyyp
            from libs.library l
            where library = 'KONTOD' 
            and l.status &lt;&gt; 3</sql>
    <alias>curKontod</alias>
  </grid>
  <select>
    <sql>select coalesce(l.rekvid,0) as rekvid, 
                case when l.tun5 = 1 then 'SD' when l.tun5 = 2 then 'SK' when l.tun5 = 3 then 'D' when l.tun5 = 4 then 'K' else null end::varchar(20) as konto_tyyp, 
                l.id, trim(l.kood)::varchar(20) as kood, trim(l.nimetus)::varchar(254) as nimetus, 
                l.library, l.tun1, l.tun2, l.tun3, l.tun4, l.muud, $2::integer as userid, 
                'KONTOD' as doc_type_id, l.tun5 as tyyp, 
                l.status,
                (l.properties::jsonb -&gt;&gt; 'valid')::date as valid
                from libs.library l 
                where id = $1</sql>
    <sqlAsNew>select null::integer as rekvId, 
            'SD'::varchar(20) as konto_tyyp, 
            $1::integer as id , $2::integer as userid, 'KONTOD' as doc_type_id,
            null::varchar(20) as  kood,
            null::varchar(20) as nimetus,
            'KONTOD'::text as library,
            null::integer as tun1,
            null::integer as tun2,
            null::integer as tun3,
            null::integer as tun4,
            2 as tyyp,
            0 as status,
            null::text as muud,
            null::date as valid</sqlAsNew>
    <query />
    <multiple>false</multiple>
    <alias>row</alias>
  </select>
  <select>
    <sql>SELECT s.*,  
                left(a.nimetus,120)::varchar AS nimetus 
                FROM  libs.subkonto s 
                INNER JOIN   libs.asutus a on a.id = s.asutusid 
                inner join ou.userid u on u.id = $2
                WHERE  s.kontoid = $1 
                and s.rekvid = u.rekvId</sql>
    <query />
    <multiple>true</multiple>
    <alias>subkonto</alias>
  </select>
  <select>
    <sql>SELECT $1 AS rekv_id, *
                  FROM jsonb_to_recordset(
                               get_konto_kasutus($2::INTEGER, $3::DATE)
                           ) AS x (error_message TEXT, error_code INTEGER)
                  WHERE error_message IS NOT NULL
            </sql>
    <query />
    <multiple>true</multiple>
    <alias>validate_lib_usage</alias>
  </select>
  <selectAsLibs>
    <sql>select *, $1 as rekv_id from com_kontoplaan l order by kood</sql>
    <alias>selectAsLibs</alias>
  </selectAsLibs>
  <saveDoc>
    <sql>select libs.sp_salvesta_konto($1, $2, $3) as id</sql>
    <alias>saveDoc</alias>
  </saveDoc>
  <deleteDoc>
    <sql>select error_code, result, error_message from libs.sp_delete_konto($1, $2)</sql>
    <alias>deleteDoc</alias>
  </deleteDoc>
  <requiredFields>
    <validate>kood,nimetus</validate>
  </requiredFields>
  <executeSql>
    <sql></sql>
    <alias></alias>
  </executeSql>
  <executeCommand>
    <sql></sql>
    <alias></alias>
  </executeCommand>
  <register>
    <sql></sql>
    <alias></alias>
  </register>
  <endProcess>
    <sql />
    <alias />
  </endProcess>
  <generateJournal>
    <sql />
    <alias />
  </generateJournal>
  <print></print>
</VFPData>
